class V1Query
    constructor: (@v1instance, @assettype, @options={}) ->
        undefined
    execute: () ->
        # assemble an url
        # make a callback that transforms the result
        #  and calls the callback in the options
        # construct http call
        # submit http call
    
    
